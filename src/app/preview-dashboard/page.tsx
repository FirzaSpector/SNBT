import DashboardLight from "../(authenticated)/dashboard/DashboardLight";

export const metadata = {
  title: "Dashboard Light Preview — SoalSNBT.id",
};

export default function PreviewPage() {
  return <DashboardLight />;
}
